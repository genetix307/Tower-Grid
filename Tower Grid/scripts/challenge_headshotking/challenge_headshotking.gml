function challenge_headshotking(){
if (store.challenge_a=6 or store.challenge_b=6 or store.challenge_c=6) and store.goal_headshotking < 10+(10*store.lvl_headshotking)
{
	store.goal_headshotking+=1		
}
}